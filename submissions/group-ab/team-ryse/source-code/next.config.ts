import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ["aiss-macbook-pro.ladon-matrix.ts.net"],
  reactCompiler: true,
};

export default nextConfig;
